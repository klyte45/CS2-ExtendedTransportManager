---
key: K45::XTM.vuio[glossary.content.map.linear.switch]
---
Select a transport route and use the **XTM map** button at the far left of the line visualizer toolbar. When selected, it replaces the game's compact route sketch with XTM's wider linear map and reveals the map overlay controls. Select it again to restore the original visualizer.

XTM map mode starts enabled. The current choice and overlay selections are retained while the mod's frontend remains loaded, but they are not city or global settings and reset when the interface reloads.

While XTM data is first loading, the original route sketch remains visible.
